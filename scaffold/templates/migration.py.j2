"""create [[ spec.table_name ]] table

Generated by scaffold/generate.py for the [[ spec.app ]] app. One migration per
app: edit it like any other migration, the generator will not rewrite it.

Revision ID: [[ revision ]]
Revises: [[ down_revision or "" ]]
"""

from collections.abc import Sequence

import sqlalchemy as sa
from alembic import op

revision: str = "[[ revision ]]"
down_revision: str | None = [[ down_revision_literal ]]
branch_labels: Sequence[str] | None = None
depends_on: Sequence[str] | None = None


def upgrade() -> None:
    op.create_table(
        "[[ spec.table_name ]]",
        sa.Column("id", sa.Integer(), primary_key=True),
[% for column in columns %]
        [[ column ]],
[% endfor %]
        sa.Column("created_at", sa.DateTime(), nullable=False),
[% for constraint in constraints %]
        [[ constraint ]],
[% endfor %]
    )


def downgrade() -> None:
    op.drop_table("[[ spec.table_name ]]")
